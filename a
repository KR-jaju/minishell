$HOME
unquote_env하기 전 ~가 맨 앞에 오며,
	길이가 1이거나 혹은 ~다음에 /가 나와야 치환이 됨.

//빌트인 이외에 이렇게 작동하는게 있는가?

if (str[0] == '~' && (str[1] == '/' || str[1] == '\0'))
{
	p = Desktop
	//cd ~/Desktop 우리가 입력
	//cd ~/Desktop 환경변수에 붙음
	cd ~$p
	cd ~/Desktop
	chdir("~/Desktop");

	cd ~/$p
	cd $HOME/$p
	cd /User/jaju/Desktop
	char *new = str_join("$HOME", str + 1);
	free(str);
	str = new;
}